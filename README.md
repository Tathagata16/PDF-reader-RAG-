# small description: 
  this is a pdf reader using (rag pipeline)
  upload -> chunking -> indexing pipeline -> vector db -> user query -> similarity search -> response

# bugs:
  only a prototype, no UI
  slow response
  this is synchronous model, blocks the workflow.
